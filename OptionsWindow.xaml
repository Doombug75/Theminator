<Window x:Class="Theminator.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Theminator Options"
        Width="460" Height="180"
        MinWidth="460" MinHeight="180"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#0D1117"
        Foreground="#E6EDF3">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Themes Folder:"
                   FontSize="12" Foreground="#A8B8CC"
                   Margin="0,0,0,4"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FolderBox" Grid.Column="0"
                     IsReadOnly="True"
                     Background="#161B22"
                     VerticalContentAlignment="Center"
                     Height="28"/>
            <Button Grid.Column="2" Content="Browse..." Width="80"
                    Click="Browse_Click"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="OK" Width="80"
                    Background="#18C0B4" Foreground="#060C10"
                    FontWeight="SemiBold" Click="OK_Click"/>
        </StackPanel>
    </Grid>
</Window>
