<Window x:Class="Theminator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="THEMINATOR — ClaudetRelay Theme Editor"
        Width="960" Height="700"
        MinWidth="900" MinHeight="680"
        Background="#0D1117"
        Foreground="#E6EDF3">
    <Window.Resources>
        <Style x:Key="GroupHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#18C0B4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,8,0,2"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#A8B8CC"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1C2333"/>
            <Setter Property="Foreground" Value="#E6EDF3"/>
            <Setter Property="BorderBrush" Value="#243044"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#243044"/>
                                <Setter Property="BorderBrush" Value="#18C0B4"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#18C0B4"/>
                                <Setter Property="Foreground" Value="#060C10"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Toolbar -->
            <RowDefinition Height="*"/>     <!-- Main content -->
            <RowDefinition Height="Auto"/>  <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- ── Toolbar ── -->
        <Border Grid.Row="0" Background="#161B22"
                BorderBrush="#1C2333" BorderThickness="0,0,0,1">
            <Grid Margin="8,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Load Theme button with context menu -->
                <Button x:Name="BtnLoadTheme" Grid.Column="0"
                        Style="{StaticResource ToolbarButtonStyle}"
                        Content="Load Theme ▾"
                        Click="BtnLoadTheme_Click"/>

                <Button x:Name="BtnNew" Grid.Column="2"
                        Style="{StaticResource ToolbarButtonStyle}"
                        Content="New"
                        Click="BtnNew_Click"/>

                <Button x:Name="BtnSaveAs" Grid.Column="4"
                        Style="{StaticResource ToolbarButtonStyle}"
                        Content="Save As..."
                        Click="BtnSaveAs_Click"/>

                <!-- Theme name display -->
                <StackPanel Grid.Column="6" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Theme: " Foreground="#6E8094" FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ThemeNameDisplay" Foreground="#E8A840"
                               FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"
                               Text="Claude's Choice"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── Main area: Left panel + Right preview ── -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="240"/>
                <ColumnDefinition Width="1"/>    <!-- Splitter -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left panel -->
            <Border Grid.Column="0" Background="#0D1117">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="LeftPanel" Margin="10,6,10,10"/>
                </ScrollViewer>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="1" Background="#1C2333"
                          HorizontalAlignment="Stretch"/>

            <!-- Right panel — live preview -->
            <Border Grid.Column="2" Background="#0D1117">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="PreviewPanel" Margin="12,8,12,12"/>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- ── Status bar ── -->
        <Border Grid.Row="2" Background="#161B22"
                BorderBrush="#1C2333" BorderThickness="0,1,0,0">
            <Grid Margin="8,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusFolderText"
                           Foreground="#4D6278" FontSize="11"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           Text="No themes folder configured"/>
                <Button Grid.Column="1" Content="Options"
                        Style="{StaticResource ToolbarButtonStyle}"
                        Height="24" Padding="8,2"
                        Click="BtnOptions_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
